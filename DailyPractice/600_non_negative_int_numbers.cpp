#include <vector>
using namespace std;
class Solution {
 public:
  int findIntegers(int n) {
    vector<int> possible = {0,1};
    int count = 2;
    int currentMax = 0;
    while (currentMax < n) {
      
    }
    return count;
  }
};